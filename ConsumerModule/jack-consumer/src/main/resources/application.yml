spring:
  application:
    name: consumer-jack

server:
  port: 8766

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

feign:
  hystrix:
    enabled: true #feign自带断路器hystrix，只需要在此处打开